// Type definitions for documentation purposes (JavaScript runtime doesn't enforce these)

/*
export interface URL {
  id: number;
  original_url: string;
  short_code: string;
  click_count: number;
  created_at: string;
  last_clicked: string | null;
}

export interface CreateURLRequest {
  url: string;
  customCode?: string;
}

export interface HealthStatus {
  status: string;
  timestamp: string;
  uptime: {
    seconds: number;
    human: string;
  };
  database: {
    status: string;
    timestamp: string;
  };
  memory: {
    rss: number;
    heapTotal: number;
    heapUsed: number;
    external: number;
  };
  system: {
    nodeVersion: string;
    platform: string;
    arch: string;
  };
}

export interface ApiResponse<T> {
  data?: T;
  error?: string;
  message?: string;
}
*/
